<!DOCTYPE html>
<html lang="ko">
    <%- include('include/head', { title: 'main' })%>
    <body>
        <%- include('include/header')%>
        <!-- 메인 페이지에 접근 할 때, 최신글 20개에 대해, postNumber, postTitle, reImage(대표 이미지 경로) -->

        <div id="main">
            <div class="mainIntro">
                <H1 class="H1WWT" style="margin-bottom: 30px">오늘의 추천 코스</H1>
            </div>
            <hr style="color: #606d75" />
            <br />
            <div class="inner">
                <div class="box-wrap">
                    <!-- Boxes -->
                    <!-- "postTitle", "postNumber", "reImage -->
                    <% for(let i = 0; i < posts.length ; i++){%>
                    <div class="box">
                        <div class="img">
                            <img src="/<%=posts[i].reImage%>" alt="Hover Effect" />
                        </div>
                        <div class="info">
                            <h3 class="fs-2" style="text-align: center"><%=posts[i].postTitle%></h3>
                            <p>
                                <a href="/getPost/<%=posts[i].postNumber%>">글을 보시겠습니까?</a>
                            </p>
                        </div>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>

        <%- include('include/footer') %>
    </body>
</html>
